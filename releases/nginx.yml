---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nginx
  namespace: develop
spec:
  interval: 5m
  chart:
    spec:
      sourceRef:
        kind: GitRepository
        name: vero-charts-git
        namespace: flux-system
      chart: ./dummy-chart
  values:
    image: nginx:1.20.0 # {"$imagepolicy": "develop:nginx"}
